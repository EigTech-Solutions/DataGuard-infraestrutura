version: '3'

services:
  jarDataGuard:
    image: openjdk:11-jre-slim
    working_dir: /app
    volumes:
      - ./dataguard-1.0-SNAPSHOT-jar-with-dependencies.jar:/app/dataguard.jar
    command: java -jar dataguard.jar
